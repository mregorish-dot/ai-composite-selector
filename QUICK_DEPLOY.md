# 🚀 Быстрый деплой приложения

## Самый простой способ - Streamlit Cloud (5 минут)

### Шаг 1: Подготовка GitHub репозитория

1. Создайте аккаунт на GitHub (если нет): https://github.com
2. Создайте новый репозиторий (например, `ai-composite-selector`)

### Шаг 2: Загрузка кода

```bash
cd "/Users/egorryzhkov/Desktop/AI Double"
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_ПОЛЬЗОВАТЕЛЬ/ai-composite-selector.git
git push -u origin main
```

### Шаг 3: Деплой на Streamlit Cloud

1. Перейдите на https://share.streamlit.io/
2. Войдите через GitHub
3. Нажмите "New app"
4. Заполните:
   - **Repository**: выберите ваш репозиторий
   - **Branch**: `main`
   - **Main file path**: `app/app.py`
5. Нажмите "Deploy"

### Шаг 4: Получите ссылку

После деплоя (1-2 минуты) вы получите ссылку:
```
https://ВАШ_ПОЛЬЗОВАТЕЛЬ-ai-composite-selector-XXXXXX.streamlit.app
```

**Эту ссылку можно отправлять друзьям!** 🌐

---

## Альтернатива: Локальный сервер для тестирования

Если хотите протестировать локально перед деплоем:

```bash
cd "/Users/egorryzhkov/Desktop/AI Double/app"
streamlit run app.py --server.address=0.0.0.0 --server.port=8501
```

Затем откройте в браузере: `http://localhost:8501`

---

## Мобильные и десктопные приложения

После получения ссылки на Streamlit Cloud:

1. **Android/iOS**: Обновите URL в файлах в папке `mobile/`
2. **Windows**: Обновите URL в `desktop/windows/main.js`

---

## Важно!

После деплоя на Streamlit Cloud:
- ✅ Приложение доступно 24/7
- ✅ Работает на всех устройствах (компьютер, телефон, планшет)
- ✅ Не требует установки
- ✅ Автоматически обновляется при изменении кода

