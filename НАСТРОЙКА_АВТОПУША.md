# 🔄 Настройка автоматического пуша в GitHub

## Проблема
Сейчас `git push` не работает автоматически, потому что требуется авторизация GitHub.

## ✅ Решение: Настроить SSH ключ (1 раз, потом работает автоматически)

### Шаг 1: Проверь, есть ли SSH ключ
```bash
ls -la ~/.ssh/id_*.pub
```

Если файлы есть → переходи к Шагу 3.

### Шаг 2: Создай SSH ключ (если нет)
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```
- Нажми Enter 3 раза (используй значения по умолчанию)

### Шаг 3: Скопируй публичный ключ
```bash
cat ~/.ssh/id_ed25519.pub
```
**Скопируй весь вывод** (начинается с `ssh-ed25519...`)

### Шаг 4: Добавь ключ на GitHub
1. Открой: https://github.com/settings/keys
2. Нажми **"New SSH key"**
3. Title: `MacBook Auto Push`
4. Key: **вставь скопированный ключ**
5. Нажми **"Add SSH key"**

### Шаг 5: Измени remote на SSH
```bash
cd "/Users/egorryzhkov/Desktop/AI Double"
git remote set-url origin git@github.com:mregorish-dot/ai-composite-selector.git
```

### Шаг 6: Проверь
```bash
git push origin new-version
```

Если работает без запроса пароля → **готово!** 🎉

---

## Альтернатива: GitHub CLI (если SSH не подходит)

### Установка GitHub CLI
```bash
brew install gh
```

### Авторизация
```bash
gh auth login
```
- Выбери GitHub.com
- Выбери SSH или HTTPS
- Следуй инструкциям

После этого `git push` будет работать автоматически!

---

## После настройки

Я смогу автоматически пушить изменения командой:
```bash
git push origin new-version
```

И Streamlit Cloud автоматически обновит приложение через 1-2 минуты! 🚀
