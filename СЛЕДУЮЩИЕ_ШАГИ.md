# Ваши следующие шаги

## ✅ Что уже готово

1. ✅ **Базовая программа** (`composite_selector.py`) - работает и готова к использованию
2. ✅ **Модуль нормализации ЭМГ** - обрабатывает данные с разных аппаратов
3. ✅ **База данных композитов** - содержит все необходимые характеристики
4. ✅ **Примеры использования** - готовые примеры кода

---

## 🚀 Шаг 1: Установка и первый запуск (5 минут)

### 1.1. Установите зависимости:

Откройте терминал в папке проекта и выполните:

```bash
cd "/Users/egorryzhkov/Desktop/AI Double"
pip3 install numpy pandas scikit-learn
```

### 1.2. Запустите программу:

```bash
python3 composite_selector.py
```

Вы должны увидеть результаты выбора композита для тестового пациента.

### 1.3. Попробуйте примеры:

```bash
python3 example_usage.py
```

---

## 📊 Шаг 2: Сбор клинических данных (1-2 недели)

### Что нужно собрать:

Для каждого пациента записывайте:

1. **ЭМГ-данные:**
   - Жевательная мышца (правая/левая) при акте жевания
   - Жевательная мышца (правая/левая) при максимальном сжатии
   - Височная мышца (правая/левая) при акте жевания
   - Височная мышца (правая/левая) при максимальном сжатии
   - Тип аппарата (Synapsys/Колибри)

2. **Клинические данные:**
   - Возраст
   - Тип аномалии прикуса
   - Степень стираемости (если есть)
   - MVC гиперфункция (%) и длительность (сек/мин)

3. **Результат лечения:**
   - Какой композит был выбран
   - Почему был выбран (обоснование врача)
   - Успешность реставрации (через 1, 3, 6 месяцев)

### Формат данных:

Создайте файл `clinical_data.csv` со следующими колонками:

```
patient_id,age,apparatus,masseter_r_chew,masseter_l_chew,temporalis_r_chew,temporalis_l_chew,
masseter_r_max,masseter_l_max,temporalis_r_max,temporalis_l_max,
occlusion_anomaly,wear_severity,mvc_percent,mvc_duration,
selected_composite,success_1m,success_3m,success_6m
```

---

## 🤖 Шаг 3: Обучение ML-модели (после сбора 20+ случаев)

### 3.1. Подготовка данных:

Создайте скрипт `train_model.py`:

```python
import pandas as pd
from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import train_test_split
from composite_selector import CompositeSelector

# Загрузка данных
data = pd.read_csv('clinical_data.csv')

# Подготовка признаков
selector = CompositeSelector()
# ... создание признаков из ЭМГ-данных ...

# Обучение модели
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)
model = RandomForestClassifier()
model.fit(X_train, y_train)

# Сохранение модели
import joblib
joblib.dump(model, 'composite_model.pkl')
```

### 3.2. Интеграция в программу:

Замените эвристический алгоритм на обученную модель.

---

## 📝 Шаг 4: Подготовка публикации (до 20 января)

### 4.1. Структура статьи:

1. **Введение** - актуальность, обзор литературы
2. **Материалы и методы** - описание программы, критерии выбора
3. **Результаты** - статистика использования, точность рекомендаций
4. **Обсуждение** - интерпретация результатов
5. **Заключение**

### 4.2. Необходимые материалы:

- Таблицы с результатами
- Графики ЭМГ-показателей
- Сравнение рекомендаций ИИ с решениями экспертов
- Статистический анализ

---

## 🎯 Приоритетные задачи

### На этой неделе:
- [ ] Установить зависимости и запустить программу
- [ ] Протестировать на 2-3 примерах
- [ ] Начать собирать клинические данные

### На следующей неделе:
- [ ] Собрать минимум 10-15 случаев
- [ ] Подготовить датасет
- [ ] Начать обучение модели

### До 20 января:
- [ ] Завершить сбор данных (20-30 случаев)
- [ ] Обучить и протестировать модель
- [ ] Подготовить черновик публикации

---

## 💡 Полезные команды

### Запуск программы:
```bash
python3 composite_selector.py
```

### Запуск примеров:
```bash
python3 example_usage.py
```

### Проверка установки:
```bash
python3 -c "import pandas, numpy, sklearn; print('Все установлено!')"
```

### Просмотр структуры проекта:
```bash
ls -la
```

---

## ❓ Частые вопросы

**Q: Программа не запускается, ошибка "ModuleNotFoundError"**
A: Установите зависимости: `pip3 install numpy pandas scikit-learn`

**Q: Как добавить новый композит?**
A: Отредактируйте файл `База_композитов.json` и добавьте новый элемент в массив `composites`

**Q: Можно ли использовать данные с другого ЭМГ-аппарата?**
A: Да, программа автоматически нормализует данные. Укажите тип аппарата в `apparatus`

**Q: Как улучшить точность рекомендаций?**
A: Соберите больше клинических данных и обучите ML-модель на реальных случаях

---

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте инструкцию по установке (`ИНСТРУКЦИЯ_ПО_УСТАНОВКЕ.md`)
2. Посмотрите примеры использования (`example_usage.py`)
3. Изучите план разработки (`ПЛАН_РАЗРАБОТКИ.md`)

---

**Удачи с исследованием! 🚀**

