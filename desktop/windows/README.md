# 🖥️ Windows приложение

## Сборка приложения для Windows

### Требования:
- Node.js (версия 16 или выше)
- npm

### Установка:

1. **Установите зависимости:**
   ```bash
   cd desktop/windows
   npm install
   ```

2. **Обновите ссылку в `main.js`:**
   - Откройте `main.js`
   - Замените `APP_URL` на вашу ссылку Streamlit Cloud

3. **Соберите приложение:**
   ```bash
   npm run build
   ```

4. **Результат:**
   - Установщик будет в папке `dist/`
   - Файл `.exe` для установки

### Запуск в режиме разработки:

```bash
npm start
```

---

## Поддержка Windows 7/10/11

Приложение работает на всех версиях Windows, начиная с Windows 7.

---

## Создание установщика

После сборки вы получите:
- `AI Composite Selector Setup.exe` - установщик
- Можно распространять этот файл

