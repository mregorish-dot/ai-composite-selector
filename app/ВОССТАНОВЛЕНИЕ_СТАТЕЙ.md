# 📚 Восстановление загруженных статей и PDF файлов

## ✅ Что было добавлено:

### 1. Автоматическое сохранение статей
- Все загруженные статьи автоматически сохраняются в файл `saved_articles.json`
- Статьи сохраняются при каждом добавлении (текст, PDF, ссылка)
- Включает полный текст статьи, метаданные и извлеченные знания

### 2. Сохранение PDF файлов
- Все загруженные PDF файлы сохраняются в директорию `saved_pdfs/`
- PDF сохраняется с оригинальным именем файла
- Путь к PDF сохраняется в данных статьи

### 3. Автоматическое восстановление
- При запуске приложения все сохраненные статьи автоматически загружаются
- Статьи восстанавливаются в `knowledge_extractor` для использования в системе
- PDF файлы доступны для скачивания из интерфейса

---

## 📁 Структура файлов:

```
app/
├── saved_articles.json      # Все загруженные статьи (JSON)
└── saved_pdfs/              # Директория с PDF файлами
    ├── article_1.pdf
    ├── article_2.pdf
    └── ...
```

---

## 🔄 Как это работает:

### При загрузке статьи (текст):
1. Статья обрабатывается и знания извлекаются
2. Статья добавляется в `st.session_state.articles`
3. **Автоматически сохраняется** в `saved_articles.json`

### При загрузке PDF:
1. PDF файл сохраняется в `saved_pdfs/`
2. Текст извлекается из PDF
3. Статья обрабатывается и знания извлекаются
4. Статья добавляется в `st.session_state.articles` с путем к PDF
5. **Автоматически сохраняется** в `saved_articles.json`

### При запуске приложения:
1. Загружаются предзагруженные статьи (из `preloaded_articles.py`)
2. **Автоматически загружаются** сохраненные статьи из `saved_articles.json`
3. Все статьи восстанавливаются в `knowledge_extractor`
4. База знаний обновляется

---

## 📋 Формат сохраненных данных:

### saved_articles.json:
```json
[
  {
    "title": "Название статьи",
    "authors": "Авторы",
    "year": 2024,
    "journal": "Журнал",
    "text": "Полный текст статьи...",
    "url": "https://...",
    "doi": "10.1234/...",
    "keywords": ["ключевое", "слово"],
    "source": "text_input" | "PDF" | "url",
    "pdf_filename": "article.pdf",  // только для PDF
    "pdf_path": "saved_pdfs/article.pdf"  // только для PDF
  }
]
```

---

## 🎯 Использование:

### Просмотр всех статей:
1. Перейдите в раздел **"📥 Загрузка данных"**
2. Откройте вкладку **"📋 Список статей"**
3. Увидите все статьи с метками:
   - 🔵 **Предзагружена** - статьи из `preloaded_articles.py`
   - 📥 **Загружена пользователем** - ваши загруженные статьи

### Скачивание PDF:
- В списке статей нажмите на статью с PDF
- Нажмите кнопку **"📥 Скачать PDF"**
- PDF файл будет скачан на ваш компьютер

### Ручное сохранение:
- В списке статей есть кнопка **"💾 Сохранить все статьи"**
- Нажмите для принудительного сохранения

---

## ⚠️ Важно:

1. **Файлы сохраняются автоматически** - не нужно ничего делать вручную
2. **При перезапуске приложения** все статьи восстанавливаются автоматически
3. **PDF файлы сохраняются на диск** - они не теряются при перезапуске
4. **Предзагруженные статьи** всегда доступны (из `preloaded_articles.py`)

---

## 🔍 Где найти сохраненные данные:

- **Статьи:** `app/saved_articles.json`
- **PDF файлы:** `app/saved_pdfs/`

---

## 📊 Статистика:

В интерфейсе отображается:
- Общее количество статей
- Количество предзагруженных статей
- Количество загруженных пользователем статей
- Для каждой статьи показывается источник (текст, PDF, ссылка)

---

**Система готова к использованию! Все ваши статьи и PDF файлы будут автоматически сохранены и восстановлены при следующем запуске.** 🚀

