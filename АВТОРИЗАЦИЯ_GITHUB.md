# 🔐 Авторизация на GitHub

Git требует авторизацию для загрузки кода. Есть 2 способа:

## Способ 1: GitHub CLI (Рекомендуется)

Если у вас установлен GitHub CLI:

```bash
gh auth login
```

Следуйте инструкциям на экране.

## Способ 2: Personal Access Token

1. **Создайте токен:**
   - Перейдите: https://github.com/settings/tokens
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Название: `ai-composite-selector`
   - Выберите scope: `repo` (полный доступ к репозиториям)
   - Нажмите "Generate token"
   - **Скопируйте токен** (он показывается только один раз!)

2. **Используйте токен при push:**
   ```bash
   git push -u origin main
   ```
   - Username: `mregorish-dot`
   - Password: **вставьте токен** (не пароль!)

## Способ 3: SSH ключ (Для постоянного использования)

1. **Создайте SSH ключ:**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

2. **Добавьте ключ на GitHub:**
   - Скопируйте публичный ключ: `cat ~/.ssh/id_ed25519.pub`
   - Перейдите: https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Вставьте ключ и сохраните

3. **Измените remote на SSH:**
   ```bash
   git remote set-url origin git@github.com:mregorish-dot/ai-composite-selector.git
   git push -u origin main
   ```

---

## ✅ После авторизации

Выполните:
```bash
cd "/Users/egorryzhkov/Desktop/AI Double"
git push -u origin main
```

Код будет загружен на GitHub!

