#!/bin/bash
# Проверка SSH подключения к GitHub

echo "🧪 Проверка SSH подключения к GitHub..."
echo ""

ssh -T git@github.com 2>&1

if [ $? -eq 0 ] || grep -q "successfully authenticated" <<< "$(ssh -T git@github.com 2>&1)"; then
    echo ""
    echo "✅ SSH подключение работает!"
    echo "🚀 Теперь можно пушить автоматически"
else
    echo ""
    echo "⚠️ SSH подключение не настроено"
    echo "💡 Убедись, что добавил ключ на GitHub"
fi
